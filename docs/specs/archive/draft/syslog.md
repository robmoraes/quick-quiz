- on the run result screen we will add an option that is more discreet than the
  action buttons; it should be an icon that suggests a Linux terminal. When this
  icon is clicked, a modal with a shell terminal theme must open with a prompt
  bar written as: " run@quiz $ sudo tail /var/log/syslog | grep -i ""
  and the terminal body should show the collected logs filtered from the run in
  syslog format;
