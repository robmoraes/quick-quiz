# Timeline - 2026-06-04

- 22:10-03: specs: created the objective MVP specs in `specs/`.
- 22:16-03: backend: created the base Go API architecture in `backend/`.
- 22:17-03: backend: created config, domain, app, httpapi, and in-memory stores.
- 22:18-03: backend: implemented health, runs, answers, finish, and result endpoints.
- 22:19-03: backend: implemented random selection, answer validation, statistics, and hardcore mode rules.
- 22:20-03: backend: added temporary seed data, README, tests, `gofmt`, and validation with `go test ./...`.
- 22:24-03: docs: created the daily timeline in a simple format to keep it updated.
- 23:35-03: backend: replaced the versioned seed with JSON loading from `backend/.local/questions`.
- 23:36-03: backend: added `GET /api/catalog` so the frontend can list languages and difficulties.
- 23:38-03: frontend: implemented the start screen, run, questions, visual feedback, and final result.
- 23:39-03: frontend: implemented the centered layout with desktop ad placeholders and no ads on mobile.
- 23:40-03: backend: added `.gitignore` to keep local JSON files out of Git.
- 23:41-03: validation: successfully ran `go test ./...`, `npm run lint`, and `npm run build`.
- 23:43-03: dev: started the backend on `:8080` and the frontend on `:9000`.
- 23:56-03: backend: added per-run sessions through the `X-QuickQuiz-Session-ID` header.
- 23:57-03: backend: created `POST /api/session/reset` to clear runs from the current session.
- 23:57-03: frontend: added a reset session button and id rotation in `sessionStorage`.
